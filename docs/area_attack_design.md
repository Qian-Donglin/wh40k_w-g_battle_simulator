# 範囲攻撃システム設計【オリジナルルール】

Blast・Flamer（Spread）など、複数の対象を巻き込む攻撃の設計をここにまとめる。
Magnitude・戦意（Morale/Rout）などHorde本体のルールは `docs/system_design.md` を正とし、
このファイルは**範囲攻撃という切り口**からの設計を扱う。

> **状態**：ルールと主要パラメータは確定。実装は未着手。
> 幾何判定には戦場の座標系が必要だが、`docs/simulator_app_design.md` にマップ・距離の定義がまだない（§7-1参照）。

---

## 1. 基本方針

範囲攻撃は**2層構造**で解決する。Hordeに当たった場合は、第1層に**加えて**第2層を適用する
（第2層は第1層を置き換えるものではない）。

| 層 | 対象 | 内容 |
|---|---|---|
| **第1層：幾何判定** | 通常キャラクター（個体）・Horde共通 | 攻撃範囲を表す図形を戦場に描き、**その図形内にいるキャラクター全員**をダメージ対象とする |
| **第2層：Horde内適用** | Hordeのみ | Hordeは個別座標を持たないため、図形とHorde占有領域の**重なり面積 × 密度**で巻き込み体数を推定する |

### 意図

線形にスケールするMagnitudeプール（`Magnitude = 個体wound × N`）を、範囲攻撃で効率的に削れるようにする。
戦意システムの較正（`docs/system_design.md` の「分岐点の設計意図」）は
**「範囲攻撃を持ち込むと殲滅側だけが加速する」ことを前提に組まれている**ため、
本ルールはその前提を実際に成立させる役割を持つ。

---

## 2. 基準密度 a ＝ 4m²【主ダイヤル】

**1体あたりの占有面積 `a = 4m²`（2m × 2m 間隔）** を全Horde共通の基準密度とする。
戦意システムにおける `k = 3.6` と同じく、範囲攻撃システム全体を動かす主ダイヤルである。

```
Horde占有面積  A_horde = N × a
密度           ρ = N / A_horde = 1 / a     ← Nに依存しない定数
```

Hordeの占有面積が人数Nに比例する以上、**密度は定数になる**。
これにより巻き込み体数は「重なり面積 ÷ a」だけで決まり、Nに依存しない。

> **サイズの妥当性**：N=100 のHordeは 400m²＝約20m四方、N=5 なら 20m²＝約4.5m四方。
> 後者にBlast Large（19.6m²）を落とすと `min(N, 5) = 5` で5体中5体が巻き込まれ、
> 「小さな群れを大爆発が飲み込む」挙動が上限式だけで自然に出る。

> **今後の拡張余地**：密集陣形・散開陣形をHordeの状態として持たせ `a` を可変にする案があるが、
> まずは定数で始める。「構成キャラのサイズによる `a` の変動」も同様に future work（§7-2）。

---

## 3. 巻き込み体数 n_hit

```
n_hit = min( N, round(重なり面積 ÷ a), Blast Sizeの対象数 )
```

最終的に四捨五入して整数にする。3項の役割は以下の通り。

| 項 | 役割 |
|---|---|
| `N` | Hordeの現在人数。いない敵は巻き込めない |
| `round(重なり面積 ÷ a)` | **幾何判定の本体** |
| `Blast Sizeの対象数` | **上限**。RAWの「グレネードは最大5人まで」という保証 |

> **上限が実際に効く場所**：Hordeに対してはほとんど効かない（半径 r を「標準密度で全面重なったらRAW対象数になる」ように逆算しているため、定義上ほぼ一致する）。
> **本当に働くのは第1層＝個体戦**で、個体は座標を持つため密集していれば密度想定を超えて巻き込まれ得る。そこでRAWの上限を効かせる。
> 扇形（フレイマー類）に対しては実際に拘束力を持つ（§5参照）。

### Blast Size の対象数（RAW）

`docs/rules/battle_rules_ja.md` §12 の表をそのまま使う。

| Size | 対象数 |
|---|---|
| Small | 1 |
| Medium | 3 |
| Large | 5 |
| Very Large | 10 |
| Huge | 15 |

---

## 4. 攻撃範囲の図形

特性ごとに図形が異なる。

| 特性 | 図形 | 起点 |
|---|---|---|
| **Blast (Size)** | **円**（半径 r） | 着弾点 |
| **Spread / Flamer** | **扇形**（角度30°、長さ L） | 射手 |

### 4-1. 円（Blast）の半径

`r = √(対象数 × a / π)` で逆算する。**幾何判定とRAWの対象数表が同じ答えを出す**ように較正してある。

| Size | RAW対象数 | 導出値 | **採用する半径 r** | 検算 `round(πr²/4)` |
|---|---|---|---|---|
| Small | 1 | 1.13m | **1m** | 1 ✓ |
| Medium | 3 | 1.95m | **2m** | 3 ✓ |
| Large | 5 | 2.52m | **2.5m** | 5 ✓ |
| Very Large | 10 | 3.57m | **3.5m** | 10 ✓ |
| Huge | 15 | 4.37m | **4.5m** | 16 → 上限15 |

半径1〜4.5mという値は実物のグレネードとして違和感がなく、
幾何方式に切り替えても公式の較正を捨てずに済む。第1層（個体への範囲攻撃）も自動的に較正される。

### 4-2. 扇形（Flamer / Spread）の角度と長さ

```
角度 θ = 30°（固定）
長さ L = 武器の射程 ÷ 2
面積   = (θ_rad / 2) × L² = 0.2618 × L²
```

**射程をそのまま使うと破綻する。** 面積は L の**2乗**で効くため、角度を絞っても追いつかない：

| 射程16mのまま | 面積 | n_hit |
|---|---|---|
| 60° | 134 m² | 33.5体 |
| 30° | 67 m² | 16.8体 |
| 15° | 33.5 m² | 8.4体（15°は16m先で幅4.2m、もはや扇形に見えない） |

そこで **L = 射程 ÷ 2** とする。射程＝噴射を届かせられる最大距離、L＝実際に致死的な炎の長さ、という解釈。
これで30°の扇という見た目を保ったまま面積が1/4に落ちる。

| 武器 | Blast | 射程 | L | 幾何 n | 上限 | **n_hit** |
|---|---|---|---|---|---|---|
| Hand Flamer | Small(1) | 12m | 6m | 2.36 | 1 | **1** |
| Flamer | Medium(3) | 16m | 8m | 4.19 | 3 | **3** ← 上限が効く |
| Heavy Flamer | Large(5) | 16m | 8m | 4.19 | 5 | **4** ← 上限は効かない |

§5の「Spreadなし」によりダメージ差が対Hordeで消えるため、
**上限が Flamer にだけ拘束をかけることが Heavy Flamer との序列を生んでいる**。
上限を扇形に適用しないと、同じ射程・同じLの両者が完全に横並びになる。

---

## 5. ダメージ解決【範囲攻撃はSpreadなし】

```
プールへの入力 = n_hit × min( 個体のwound, max(0, ダメージ - Resilience) )
                              ↑ 1ヒットあたり1体分で打ち切り。超過分は捨てる
```

- **ダメージロールは1回だけ振り、n_hit 倍する。** 対象は全て同一ステータスなので、
  n_hit 回振っても期待値は同じで分散が変わるだけ。処理の軽さを取る。
- **Resilience は1ヒットにつき1回引く**（`docs/system_design.md` の決定に従う）。
  範囲攻撃は「n_hit 回の1ヒットが発生した」扱いなので、n_hit 体それぞれから引かれることになる。
- **範囲攻撃には常時Spread（オーバーキルの波及）を適用しない。**
  面積で n 倍している上にオーバーキルまで波及させると範囲効果を二重取りするため。
  判定は `n_hit` の値ではなく **Blast / Spread 特性を持つかどうか**で行う。
  特性を持たない単体攻撃には従来通り常時Spreadを適用する（`docs/system_design.md`）。

### 帰結：範囲武器は「ダメージ」ではなく「面積」で決まる武器になる

ダメージが Horde の個体wound を超えた時点で頭打ちになるため、
**撃破数がダメージ値ではなく n_hit で決まる**。副産物として武器の性格分けが自動で出る：

| 武器 | Blast | ダメージ | 対Horde撃破数 |
|---|---|---|---|
| フラググレネード | Medium(3) | 10+1ED | **3.0体** |
| クラックグレネード | Small(1) | 14+2ED | **1.0体** |

ダメージは上なのに対集団性能は1/3。対人 vs 対戦車という本来の役割分担が追加ルールなしで成立する。

---

## 6. 検算

**基準Horde**：個体 wound 6・Resilience 4（Toughness 3 相当）。ボルトガン Salvo 2 を通常火力の基準とする。

| 攻撃 | 計算 | 撃破数／アクション |
|---|---|---|
| ボルトガン(10+1ED) Salvo 2 | (10.5−4)×2 = 13、波及あり | 2.2体 |
| Hand Flamer(7+1ED) | 1 × min(6, 3.5) = 3.5 | 0.6体 |
| Flamer(10+1ED) | 3 × min(6, 6.5) = 18 | 3.0体 |
| Heavy Flamer(12+2ED) | 4 × min(6, 9) = 24 | 4.0体 |
| フラググレネード | 3 × min(6, 6.5) = 18 | 3.0体 |
| クラックグレネード | 1 × min(6, 11) = 6 | 1.0体 |

**バランス曲線**（4人パーティ。「範囲攻撃あり」は2人がフレイマー系）

| N | 通常火力 | 範囲攻撃あり | Rout | 判定 |
|---|---|---|---|---|
| 20 | 2.3ターン | **1.8ターン** | 2.5 | HP（範囲ありで明確に速い）✓ |
| 30 | 3.5 | 2.7 | 2.9 | 戦意（通常火力なら）✓ |
| 100 | 11.5 | 8.8 | 3.8 | 戦意（圧倒的）✓ |

`docs/system_design.md` の設計意図通りの曲線になっている。

> **既知の差異**：`docs/system_design.md` の較正表は範囲攻撃で火力2倍（10→20体/ターン）を仮置きしていたが、
> 実際には**約1.3倍**に留まる（Spreadなしにしたぶん範囲武器が弱いため）。
> N=20の分岐は上表の通り成立しているので要件は満たしているが、2倍に寄せたい場合は
> `a` を下げるか扇形の角度を上げることで調整できる。

### 戦意システムへの接続

範囲攻撃の効果は**「そのターンの戦死者数の増加」としてのみ**戦意に伝わる（戦意への直接ボーナスは与えない）。
戦意ダメージのシグモイドは現実的な火力域で飽和済み（`MoraleDamage ≈ k`）なので、
火力を上げてもRoutまでのターン数は変わらず、**HP削りだけが加速する**。
これが「範囲攻撃を持ち込むと殲滅側だけが速くなる」という設計意図の実体である。

---

## 7. Future Work

| # | 項目 | 内容 | 優先度 |
|---|---|---|---|
| 1 | **戦場の座標系・マップ** | 幾何判定の前提。`docs/simulator_app_design.md` にマップ・距離・キャラ位置の定義がない。**範囲攻撃実装の最大のブロッカー** | 高 |
| 2 | **構成キャラのサイズによる `a` の変動** | 現状 `a = 4m²` 固定。W&GのSize（Tiny〜Gargantuan）に応じて変える。大型クリーチャーのHordeは占有面積が広く巻き込まれやすい | 中 |
| 3 | **図形の当たり判定実装** | 円・扇形とHorde占有領域の重なり面積計算。Horde占有領域の形（円／矩形／自由形状）も未定 | 中 |
| 4 | **陣形（密集／散開）** | Hordeの状態として持たせ `a` を可変にする | 低 |
| 5 | **外れた場合の散乱（scatter）** | RAWでは Blast は外れると着弾点がずれる（`docs/rules/battle_rules_ja.md` §12）。採用するか | 低 |
| 6 | **範囲攻撃への防御** | RAWでは Large 以下は Full Defence で回避可能、Very Large 以上は回避不可。幾何判定でどう表現するか | 低 |
| 7 | **同士討ち（味方の巻き込み）** | 円・扇形の中に味方がいた場合の扱い | 低 |

---

## 8. Spread 特性の再定義【解決済み】

以前、Spread の定義が3箇所で食い違っていた。

| 出典 | Spread の定義 |
|---|---|
| `docs/rules/weapons_ja.md`（公式和訳） | 群れの敵を倒すと**余剰ダメージが隣接する敵に及ぶ**（オーバーキルの波及） |
| `Assets/Scripts/Weapons/WeaponTrait.cs` | **扇状範囲への射撃**が可能 |

**解決**：公式和訳側の「オーバーキルの波及」は、Hordeのダメージ解決に**常時Spread**として吸収された
（`docs/system_design.md` 参照。プールを1本にしている以上、追加ルールなしで自動的に成立する）。
役割が空いた `Spread` の特性名は、**扇形の攻撃範囲**を指すものとして再定義する（`WeaponTrait.cs` の記述と一致）。

`WeaponTrait.Flamer`（「直線範囲攻撃」）は公式和訳の特性一覧に存在しない値であり、
フレイマー武器は RAW では `Blast(Size) + Spread` で表現される。本設計もこれに従う。

---

## 関連

- Horde・Magnitude・常時Spread・戦意システム → `docs/system_design.md`
- 公式ルールの範囲効果（対象数表・防御・散乱） → `docs/rules/battle_rules_ja.md` §12
- 武器特性一覧 → `docs/rules/weapons_ja.md`
- 武器データ定義 → `docs/equipments/weapon.md`